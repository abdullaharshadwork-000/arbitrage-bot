# Pinned to the versions this project is verified against.
# Update deliberately: an exchange-API change in ccxt can alter live order behaviour.
ccxt==4.5.47
Flask==3.1.3
cryptography==46.0.6
waitress==3.0.2

# This pinned CCXT build includes ``ccxt.pro``. Public WebSocket books remain
# optional; its authenticated order stream is a production-readiness gate.
